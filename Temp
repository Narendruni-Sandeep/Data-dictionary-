Panel tablePanel = new Panel(new GridLayout(1, 7)); // Adjusted for 7 columns
        listSNo = new List();
        listName = new List();
        listEmpId = new List();
        listJob = new List();
        listSalary = new List();
        listHireDate = new List();
        listDeptNo = new List();
        
        tablePanel.add(listSNo);
        tablePanel.add(listName);
        tablePanel.add(listEmpId);
        tablePanel.add(listJob);
        tablePanel.add(listSalary);
        tablePanel.add(listHireDate);
        tablePanel.add(listDeptNo);
